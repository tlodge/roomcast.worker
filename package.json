{
  "name": "buttonkit.worker",
  "version": "0.0.1",
  "scripts": {
    "dev": "node worker.js -f './conf/config-dev.json'",
    "start": "node worker.js",
    "dockerise": "docker build -t buttonkit_worker .",
    "push": "docker tag buttonkit_worker tlodge/worker && docker push tlodge/worker",
    "deploy": "npm run dockerise && npm run push"
  },
  "dependencies": {
    "amqplib": "^0.3.2",
    "command-line-args": "^4.0.7",
    "superagent": "^1.2.0"
  }
}
